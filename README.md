# k8s-cicd-github-action-pipeline

A demo project showing a complete CI/CD pipeline using GitHub Actions and Kubernetes.
Every push to main automatically builds a Docker image, pushes it to Docker Hub,
and deploys it to a Kubernetes cluster.
